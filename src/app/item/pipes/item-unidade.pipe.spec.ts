import { ItemUnidadePipe } from './item-unidade.pipe';

describe('ItemUnidadePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemUnidadePipe();
    expect(pipe).toBeTruthy();
  });
});
