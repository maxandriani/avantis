import { BusinessValidatorDirective } from './business-validator.directive';

describe('BusinessValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new BusinessValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
