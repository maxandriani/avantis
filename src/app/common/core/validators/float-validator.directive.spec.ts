import { FloatValidatorDirective } from './float-validator.directive';

describe('FloatValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
